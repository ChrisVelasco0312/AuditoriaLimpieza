import { shallowMount } from '@vue/test-utils'
import App from '@/App.vue'

describe('App.vue', () => {
  xit('example test', () => {
    // const wrapper = shallowMount(App)
    // const container = wrapper.find('.root-container')
    // expect(wrapper.text()).toMatch(msg)
  })
})
